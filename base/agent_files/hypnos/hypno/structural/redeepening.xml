<!-- Default redeepening: a short drop back into trance — glob pool, one variant per playback.
     Dropping a new .xml into content/redeepening_pools/ adds it to the pool automatically. -->
<include src='../content/redeepening_pools/*.xml'/>
