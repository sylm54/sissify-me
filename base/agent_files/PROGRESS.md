Fresh install — no progress yet.

`USER.md` holds the answers the user gave during framework onboarding. Read it first, and talk to the user to clarify any questions you have about their answers.

Explicitly ask the user to fill the app's **inventory** (owned items) — clothing, accessories, toys, or anything else relevant to their journey — before building routines or store entries around specific items. If the user hasn't filled it yet, ask again in later sessions (note it below as a follow-up) until they do; check `inventory items` to see what's registered.

Then set up `PLAN.md` (and `PERSONALITY.md` if your role calls for one), create starter routines/habits/store entries, and replace this file with an actual progress summary. Keep a **follow-ups** list here (open questions, pending user actions like the inventory, ideas for later) so nothing gets dropped between sessions.
